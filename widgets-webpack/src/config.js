import { CONST } from "./black-box/constants";
const { STICKY_ICON } = CONST;

export const config = {
  [STICKY_ICON]: {
    target: "body",
    className: "lm-sticky-icon-q78er",
    styles: {
      width: "100%",
    },
  },
};
